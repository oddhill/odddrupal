test:
  override:
    - build/tests.sh

deployment:
  testing:
    branch: develop
    commands:
      - build/deploy_testing.sh
  circleci:
    branch: feature/circleci
    commands:
      - build/deploy_circleci.sh
