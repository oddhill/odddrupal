<?php

/**
 * Implements hook_page_alter().
 */
function poolbar_page_alter(&$page) {
  global $user;
  
  // Remove the toolbar for user 1.
  if ($user->uid == 1) {
    unset($page['page_top']['toolbar']);
  }
}
